import { ref } from "vue";

// Define global variables
export const trainingSettingsFlag = ref(true); // Define flag, true: settings visible, false: settings invisible
export const modelDetailPrepared = ref(false); 
// export const VERSION = '1.0.0';